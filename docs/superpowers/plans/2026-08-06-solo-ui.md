# 혼자서 모드 화면 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3단계에서 만든 규칙 층 위에 화면을 얹어 혼자서 모드를 실제로 플레이할 수 있게 한다.

**Architecture:** `app/_solo/*` 의 순수 함수(`economy.ts`, `vault.ts`, `storage.ts`)는 이미 있다. 화면은 그걸 부르기만 한다. 한 페이지 안에서 `phase` 상태로 단계를 바꾸는 기존 `game.tsx` 방식을 따르되, **파일을 쪼개서** 1103줄짜리를 또 만들지 않는다.

**Tech Stack:** Next.js 16, React 19, Tailwind v4. 새 의존성 없음.

**설계 문서:** `docs/superpowers/specs/2026-08-05-solo-mode-design.md` — "화면" 절이 확정안이다.
**시안(렌더된 HTML):** `/private/tmp/claude-501/-Users-seungboshim-Projects-personal-kbo-card-gacha/717265b9-f644-4e39-acc2-8229ea1901cc/scratchpad/mock.html`
문구·간격·배치의 참조로 쓴다. **CSS 는 손으로 쓴 것이라 클래스를 그대로 옮기지 말고** Tailwind 로 다시 쓴다.

---

## 이미 있는 것 (3단계 산출물, 다시 만들지 말 것)

`app/_solo/economy.ts`
- `START_CREDITS` (2000), `PACKS` (일반 400/3장, 고급 1200/5장, 플래티넘 3000/5장), `MAX_PLUS` (10)
- `cardValue(tier, plus)` — 판매가이자 성과 점수
- `oddsAt(plus, guard)` → `{ success, keep, destroy }` (%)
- `upgradeCost(tier, plus)`, `guardFee(tier, plus)`
- `isBankrupt(credits, vaultSize)`, `cheapestPackPrice()`, `packExpectedValue(pack)`

`app/_solo/vault.ts`
- `Owned = { id, plus }`, `Slot = { id, plus, count }`, `SlotRef = { id, plus }`
- `toSlots(vault)`, `takeFrom(vault, ref, n)`, `applyUpgrade(vault, ref, "success"|"keep"|"destroy")`

`app/_solo/storage.ts`
- `Run`, `newRun(season)`, `loadRun(season)`, `saveRun(run)`, `clearRun(season)`

`app/_game/deck.ts` — `drawPack(byTier, n, rnd, rates)` 가 팩별 확률표를 받는다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `app/_game/pack.tsx` | 팩 껍데기. `game.tsx` 에서 꺼내 두 모드가 같이 쓴다 |
| `app/_sports/season-list.tsx` | 시즌 고르기 목록. `multi`·`solo` 두 모드가 같이 쓴다 |
| `app/[sport]/solo/page.tsx` | 혼자서 시즌 고르기 |
| `app/[sport]/solo/[season]/page.tsx` | 서버. 풀을 읽어 넘긴다 |
| `app/_solo/solo.tsx` | 화면 전체를 지휘. 런 상태와 phase 만 들고 나머지는 아래에 맡긴다 |
| `app/_solo/shop.tsx` | 팩 세 칸 |
| `app/_solo/vault-grid.tsx` | 보관함 도감과 고르기 |
| `app/_solo/sell-modal.tsx` | 판매 확인 |
| `app/_solo/upgrade-overlay.tsx` | 강화 |
| `app/_solo/result.tsx` | 런 종료 화면 |

**파일을 쪼개는 이유가 있다.** `game.tsx` 가 1103줄이라 한 곳을 고치면 다른 조합이 같이 움직인다.
CLAUDE.md 도 이걸 경고한다. 혼자서 모드는 처음부터 화면별로 가른다.

`card.tsx` 의 `PlayerCard` 에 `plus?: number` prop 을 더한다. 기존 호출부는 안 건드린다.

---

### Task 1: 팩 껍데기를 꺼낸다

혼자서 모드도 팩을 뜯는데 지금은 `game.tsx` 안에 박혀 있다. 동작은 그대로 두고 자리만 옮긴다.

**Files:**
- Create: `app/_game/pack.tsx`
- Modify: `app/_game/game.tsx`

- [ ] **Step 1: 옮길 것을 확인한다**

`app/_game/game.tsx` 에서 이 조각들이 팩 껍데기 전용이다. 다른 데서 안 쓰이는지 먼저 grep 으로 확인하고 옮긴다.

- `PACK_THEME` (89번째 줄 근처)
- `TEETH`, `TOOTH_D`, `TEAR_STEPS`, `TEAR_D` (118~121)
- `packClip()` (127)
- `TEAR_X`, `CLIP_WHOLE`, `CLIP_LEFT`, `CLIP_RIGHT` (144~147)
- `PackShell` 컴포넌트 (396~453) — `Game` 안의 내부 함수다

- [ ] **Step 2: `app/_game/pack.tsx` 를 만든다**

위 조각들을 그대로 옮긴다. `PackShell` 은 `Game` 의 클로저에서 `sport` 와 `packSize` 를 읽고 있었으므로 **prop 으로 받게 바꾼다.** 그 외 JSX 는 한 글자도 바꾸지 않는다.

```tsx
"use client";

import type { SportConfig } from "./deck";

// (여기에 PACK_THEME, TEETH, TOOTH_D, TEAR_STEPS, TEAR_D, packClip, TEAR_X,
//  CLIP_WHOLE, CLIP_LEFT, CLIP_RIGHT 를 game.tsx 에서 그대로 옮긴다.
//  주석도 같이 옮긴다. 값은 실측으로 맞춘 것이라 건드리지 않는다.)

/**
 * 비닐 카드팩 껍데기. 여럿이서 모드의 고르기·개봉과 혼자서 모드의 상점·개봉이 같이 쓴다.
 * 겉모습이 같아야 "그 팩을 뜯는다"는 연결이 유지된다.
 * clip 으로 좌/우 조각만 남기면 뜯기 연출에서 두 쪽으로 쪼갤 수 있다.
 */
export function PackShell({
  sport,
  packSize,
  dim,
  clip,
  animateClass,
}: {
  sport: SportConfig;
  packSize: number;
  dim?: boolean;
  clip?: "left" | "right";
  animateClass?: string;
}) {
  // (game.tsx 의 PackShell 본문을 그대로. sport 와 packSize 는 이제 prop 이다.)
}
```

- [ ] **Step 3: `game.tsx` 에서 걷어내고 새 컴포넌트를 쓴다**

옮긴 조각들을 `game.tsx` 에서 지우고 import 를 더한다.

```tsx
import { PackShell } from "./pack";
```

`game.tsx` 안의 `<PackShell ... />` 호출 세 자리(picking 의 `<PackShell dim={...} />`, opening 의 `pack-in`, `pack-tear-left`/`pack-tear-right`)에 `sport={sport} packSize={packSize}` 를 더한다.

- [ ] **Step 4: 여럿이서 모드가 안 깨졌는지 확인한다**

Run: `npm run build && npm run dev -- -p 3100`

`http://localhost:3100/kbo/multi/2026` 에서 2인으로 시작해 팩을 고르고 개봉까지 본다. 확인할 것:

- 팩 겉모습이 전과 같다 (노란 바탕, 야구공 실밥 두 줄, "카드깡 / 2026 KBO" 밴드, "선수 카드 5장 / 등급 무작위")
- 고른 팩이 어두워진다(`dim`)
- 뜯기 연출에서 좌우로 쪼개져 날아간다
- `http://localhost:3100/epl/multi/2526` 은 보라 바탕에 축구공 무늬다

**겉모습이 조금이라도 달라지면 옮기다 놓친 것이 있다.** 멈추고 보고한다.

- [ ] **Step 5: 검증과 커밋**

Run: `npm test && npm run lint && npx tsc --noEmit`

```bash
git add app/_game/pack.tsx app/_game/game.tsx
git commit -m "팩 껍데기를 game.tsx 에서 꺼낸다"
```

---

### Task 2: 시즌 고르기를 공용화하고 혼자서 라우트를 연다

`/[sport]/multi` 의 시즌 목록을 `solo` 도 쓴다. 두 번째 호출자가 생겼으니 이제 뺀다.

**Files:**
- Create: `app/_sports/season-list.tsx`
- Modify: `app/[sport]/multi/page.tsx`
- Create: `app/[sport]/solo/page.tsx`

- [ ] **Step 1: 공용 컴포넌트를 만든다**

`app/[sport]/multi/page.tsx` 의 JSX 를 거의 그대로 옮기되 모드를 prop 으로 받는다.

```tsx
import Link from "next/link";
import { SPORT, seasonsOf, type Season } from "./seasons";

/**
 * 시즌 고르기 목록. 여럿이서·혼자서 두 모드가 같이 쓴다.
 * 시즌이 하나뿐이어도 이 화면을 보여준다. PL 26/27 이 곧 열리고 KBO 2026 도 내년이면
 * 지난 시즌이 된다. 시즌이 하나인 상태는 지금 이 순간뿐이다.
 */
export function SeasonList({
  sport,
  mode,
  modeLabel,
}: {
  sport: Season["sport"];
  mode: "solo" | "multi";
  modeLabel: string;
}) {
  const seasons = seasonsOf(sport);
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-lg flex-col justify-center gap-8 px-4 py-10">
      <div>
        <Link
          href="/"
          className="rounded text-sm text-zinc-500 transition-colors outline-none hover:text-zinc-300 focus-visible:ring-2 focus-visible:ring-white/70"
        >
          ← 카드깡
        </Link>
        <h1 className="mt-3 text-2xl font-black tracking-tight">
          {SPORT[sport].emblem} {SPORT[sport].name} · {modeLabel}
        </h1>
        <p className="mt-1.5 text-sm text-zinc-400">시즌을 골라주세요</p>
      </div>

      <div className="flex flex-col gap-3">
        {seasons.map((s) => (
          <Link
            key={s.id}
            href={`/${sport}/${mode}/${s.id}`}
            className="rounded-2xl bg-white/5 px-5 py-4 ring-1 ring-white/10 transition-colors hover:bg-white/10 focus-visible:ring-2 focus-visible:ring-white/70 focus-visible:outline-none"
          >
            <div className="flex items-center gap-2">
              <span className="text-xl font-black tabular-nums">{s.label}</span>
              {s.live && (
                <span className="rounded-full bg-rose-500/15 px-2 py-0.5 text-[10px] font-bold text-rose-300 ring-1 ring-rose-400/40 ring-inset">
                  LIVE
                </span>
              )}
            </div>
            <p className="mt-1 text-xs text-zinc-400">
              {s.live
                ? "매일 기록이 갱신돼요. 어제보다 등급이 오른 선수는 카드에 표시가 붙어요."
                : "시즌이 끝나 등급이 고정된 카드예요."}
            </p>
          </Link>
        ))}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: `multi/page.tsx` 가 이걸 쓰게 줄인다**

```tsx
import { notFound } from "next/navigation";
import { SPORT, SPORT_KEYS } from "../../_sports/seasons";
import { SeasonList } from "../../_sports/season-list";

export const dynamicParams = false;

export function generateStaticParams() {
  return SPORT_KEYS.map((sport) => ({ sport }));
}

export async function generateMetadata({ params }: { params: Promise<{ sport: string }> }) {
  const { sport } = await params;
  const s = SPORT[sport as keyof typeof SPORT];
  return { title: s ? `${s.name} 여럿이서 · 시즌 고르기` : "카드깡" };
}

export default async function Page({ params }: { params: Promise<{ sport: string }> }) {
  const { sport } = await params;
  if (!(sport in SPORT)) notFound();
  return <SeasonList sport={sport as keyof typeof SPORT} mode="multi" modeLabel="여럿이서" />;
}
```

- [ ] **Step 3: `app/[sport]/solo/page.tsx` 를 만든다**

위와 같되 `mode="solo"`, `modeLabel="혼자서"`, 제목은 `혼자서 · 시즌 고르기`.

- [ ] **Step 4: 확인**

Run: `npm run build && npm run dev -- -p 3100`

- `/kbo/multi` 가 전과 똑같이 나온다
- `/kbo/solo` 가 같은 모양으로 나오고 제목만 "혼자서" 다
- `/kbo/solo/2026` 은 아직 404 다 (다음 Task 에서 만든다)

- [ ] **Step 5: 검증과 커밋**

```bash
git add app/_sports/season-list.tsx "app/[sport]/multi/page.tsx" "app/[sport]/solo/page.tsx"
git commit -m "시즌 고르기를 두 모드가 같이 쓰게 뺀다"
```

---

### Task 3: 상점과 보관함 뼈대

팩을 사면 카드가 보관함에 바로 쌓이는 최소 형태를 만든다. 개봉 연출·판매·강화는 다음 Task 들에서 얹는다.

**Files:**
- Create: `app/[sport]/solo/[season]/page.tsx`
- Create: `app/_solo/solo.tsx`
- Create: `app/_solo/shop.tsx`
- Modify: `app/_game/card.tsx` (`plus` prop)
- Modify: `app/page.tsx` (혼자서 열기)

- [ ] **Step 1: `card.tsx` 에 강화 수치를 더한다**

`PlayerCard` 의 props 에 `plus`를 더한다.

```tsx
export function PlayerCard({
  card,
  sport,
  size = "compact",
  plus = 0,
}: {
  card: Card;
  sport: SportConfig;
  size?: CardSize;
  /** 강화 수치. 0 이면 아무것도 안 그린다. 팩에서 막 나온 카드가 압도적으로 많아서,
   *  그 다수에 표시가 없어야 손을 탄 소수가 튄다. */
  plus?: number;
}) {
```

이름 줄 두 자리(hero 레이아웃의 `.nameline`, 기본 레이아웃의 이름 줄)에 칩을 더한다. 두 곳 다 `card.back` 을 그리는 바로 뒤다.

```tsx
{plus > 0 && (
  <span
    className={`shrink-0 rounded-md px-1.5 text-[11px] leading-relaxed font-black tabular-nums ring-1 ring-inset ${s.label}`}
    style={{ background: "rgba(9,9,11,.75)" }}
  >
    +{plus}
  </span>
)}
```

그리고 이름 줄의 `items-baseline` 을 `items-center` 로 바꾼다. 글자 크기가 달라 baseline 으로 두면 칩이 아래로 처진다.

**`mini` 크기에서 이름이 밀리지 않는지 반드시 눈으로 확인한다.** `mini` 는 폭이 165px 안팎이다.

- [ ] **Step 2: 서버 페이지**

`app/[sport]/solo/[season]/page.tsx` 는 `multi/[season]/page.tsx` 와 같은 모양이되 `<Solo>` 를 그린다.

```tsx
import { notFound } from "next/navigation";
import Solo from "../../../_solo/solo";
import { loadPool } from "../../../_sports/pools";
import { SEASONS, SPORT, seasonKey, type Season } from "../../../_sports/seasons";

export const dynamicParams = false;

export function generateStaticParams() {
  return SEASONS.map((s) => ({ sport: s.sport, season: s.id }));
}

const find = (sport: string, season: string): Season | undefined =>
  SEASONS.find((s) => s.sport === sport && s.id === season);

export async function generateMetadata({ params }: { params: Promise<{ sport: string; season: string }> }) {
  const { sport, season } = await params;
  const s = find(sport, season);
  return { title: s ? `${SPORT[s.sport].name} 혼자서 ${s.label}` : "카드깡" };
}

export default async function Page({ params }: { params: Promise<{ sport: string; season: string }> }) {
  const { sport, season } = await params;
  const s = find(sport, season);
  if (!s) notFound();
  const pool = await loadPool(seasonKey(s));
  return <Solo pool={pool} sport={s.sport} season={seasonKey(s)} />;
}
```

- [ ] **Step 3: `app/_solo/shop.tsx`**

```tsx
"use client";

import { PACKS, type Pack } from "./economy";

export function Shop({ credits, onBuy }: { credits: number; onBuy: (pack: Pack) => void }) {
  return (
    <section className="flex flex-col gap-3">
      <h2 className="text-sm font-bold tracking-wide text-zinc-500 uppercase">상점</h2>
      <div className="grid gap-3 sm:grid-cols-3">
        {PACKS.map((p) => {
          const afford = credits >= p.price;
          return (
            <button
              key={p.key}
              type="button"
              disabled={!afford}
              onClick={() => onBuy(p)}
              className="flex flex-col gap-1 rounded-2xl bg-white/5 px-4 py-4 text-left ring-1 ring-white/10 transition-colors outline-none hover:bg-white/10 focus-visible:ring-2 focus-visible:ring-white/70 disabled:cursor-not-allowed disabled:opacity-40 disabled:hover:bg-white/5"
            >
              <div className="flex items-baseline justify-between gap-2">
                <span className="text-sm font-bold">{p.name}</span>
                <span className="text-sm font-black text-amber-300 tabular-nums">{p.price.toLocaleString()}</span>
              </div>
              <span className="text-xs text-zinc-400">{p.blurb}</span>
              <span className="text-[11px] text-zinc-600">{p.size}장</span>
            </button>
          );
        })}
      </div>
    </section>
  );
}
```

- [ ] **Step 4: `app/_solo/solo.tsx` 뼈대**

런 상태를 들고 localStorage 와 동기화한다. 팩을 사면 카드를 뽑아 보관함에 넣는다. 이 단계에서는 개봉 연출 없이 바로 들어간다.

```tsx
"use client";

import { useEffect, useState } from "react";
import { PlayerCard } from "../_game/card";
import { groupByTier, type Card, type SportConfig } from "../_game/deck";
import { KBO } from "../_sports/kbo";
import { EPL } from "../_sports/epl";
import { drawPack } from "../_game/deck";
import { cardValue, type Pack } from "./economy";
import { toSlots } from "./vault";
import { loadRun, newRun, saveRun, type Run } from "./storage";
import { Shop } from "./shop";

const SPORTS: Record<SportConfig["key"], SportConfig> = { kbo: KBO, epl: EPL };

export default function Solo({
  pool,
  sport: sportKey,
  season,
}: {
  pool: Card[];
  sport: SportConfig["key"];
  season: string;
}) {
  const sport = SPORTS[sportKey];
  // 서버 렌더에서는 localStorage 를 못 읽으므로 새 런으로 시작했다가,
  // 브라우저에 붙은 뒤 저장된 런이 있으면 갈아끼운다. 그래야 하이드레이션이 안 어긋난다.
  const [run, setRun] = useState<Run>(() => newRun(season));
  const [ready, setReady] = useState(false);

  useEffect(() => {
    setRun(loadRun(season) ?? newRun(season));
    setReady(true);
  }, [season]);

  useEffect(() => {
    if (ready) saveRun(run);
  }, [run, ready]);

  const byId = new Map(pool.map((c) => [c.id, c]));
  const slots = toSlots(run.vault);

  function buy(pack: Pack) {
    if (run.credits < pack.price) return;
    const picked = drawPack(groupByTier(pool), pack.size, Math.random, pack.rates);
    setRun((r) => ({
      ...r,
      credits: r.credits - pack.price,
      vault: [...r.vault, ...picked.map((c) => ({ id: c.id, plus: 0 }))],
    }));
  }

  return (
    <main className="mx-auto flex min-h-screen w-full max-w-3xl flex-col gap-8 px-4 py-6">
      <header className="flex items-center justify-between gap-4">
        <h1 className="text-lg font-black tracking-tight">
          {sport.title} <span className="text-zinc-500">혼자서</span>
        </h1>
        <span className="text-base font-black text-amber-300 tabular-nums">
          <span className="mr-1.5 text-xs font-medium text-zinc-500">보유</span>
          {run.credits.toLocaleString()}
        </span>
      </header>

      <Shop credits={run.credits} onBuy={buy} />

      <section className="flex flex-col gap-3">
        <h2 className="text-sm font-bold tracking-wide text-zinc-500 uppercase">
          보관함 <span className="text-zinc-600">{slots.length}칸 · {run.vault.length}장</span>
        </h2>
        {slots.length === 0 ? (
          <p className="py-10 text-center text-sm text-zinc-600">아직 카드가 없어요. 팩을 사보세요.</p>
        ) : (
          <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">
            {slots.map((s) => {
              const card = byId.get(s.id);
              if (!card) return null;
              return (
                <div key={`${s.id}-${s.plus}`} className="flex flex-col gap-1.5">
                  <PlayerCard card={card} sport={sport} size="mini" plus={s.plus} />
                  <div className="flex items-center justify-between px-0.5">
                    <span className="text-[11px] font-bold tabular-nums text-zinc-400">
                      {cardValue(card.tier, s.plus).toLocaleString()}
                    </span>
                    {s.count > 1 && (
                      <span className="rounded bg-white/7 px-1.5 text-[10px] font-bold text-zinc-500 tabular-nums">
                        {s.count}장
                      </span>
                    )}
                  </div>
                </div>
              );
            })}
          </div>
        )}
      </section>
    </main>
  );
}
```

**복원 안 되는 id 는 조용히 건너뛴다**(`byId.get` 이 없으면 `null`). 커트를 고정해서 거의
안 일어나지만, 방출된 선수가 있을 수 있다.

- [ ] **Step 5: 메인화면에서 혼자서를 연다**

`app/page.tsx` 의 `MODES` 에서 solo 의 `ready` 를 `true` 로 바꾸고, 그 옆 주석("다음 단계에서 열린다")을 지운다.

- [ ] **Step 6: 확인**

Run: `npm run build && npm run dev -- -p 3100`

`http://localhost:3100/` → 혼자서 KBO → 2026 으로 들어간다. 확인할 것:

- 보유 크레딧이 2,000 이다
- 플래티넘 팩(3,000)이 흐리게 잠겨 있고 일반·고급은 살 수 있다
- 일반팩을 사면 크레딧이 1,600 이 되고 카드 세 장이 보관함에 뜬다
- 같은 선수가 또 나오면 한 칸에 "2장" 이 붙는다
- 카드 아래 값이 등급에 맞는다 (커먼 20, 언커먼 80, 레어 250, 에픽 800, 레전드 3,000)
- **새로고침해도 크레딧과 보관함이 그대로다**
- 콘솔에 하이드레이션 경고가 없다

- [ ] **Step 7: 검증과 커밋**

Run: `npm test && npm run lint && npx tsc --noEmit`

```bash
git add app/_game/card.tsx app/page.tsx app/_solo/solo.tsx app/_solo/shop.tsx "app/[sport]/solo/[season]/page.tsx"
git commit -m "혼자서 모드의 상점과 보관함을 만든다"
```

---

### Task 4: 고르기와 판매

**Files:**
- Create: `app/_solo/vault-grid.tsx`
- Create: `app/_solo/sell-modal.tsx`
- Modify: `app/_solo/solo.tsx`

- [ ] **Step 1: `vault-grid.tsx`**

보관함 도감과 고르기를 여기로 옮긴다. 길게 누르기로 고르기에 들어가고, 헤더 버튼으로도 들어간다.

핵심 요구사항(설계 문서 "고르기" 절):

- 평상시: 짧게 누르면 카드 확대. 카드 아래에 값과 장수
- 고르기: 모든 칸에 수량 핸들. `0/1` 이면 안 고른 것. 1 에서 `−` 를 누르면 0 이 되며 풀린다
- 길게 누르기 500ms. **손가락이 10px 넘게 움직이면 취소한다** (스크롤과 부딪힌다)
- `-webkit-touch-callout: none`, `user-select: none`, `contextmenu` 막기
- 고르기 중 카드는 `role="checkbox"` 에 `aria-checked`
- 하단 고정 바에 판매·강화. **강화는 한 칸에서 한 장만 골랐을 때만 활성**

길게 누르기 훅은 이렇게 둔다.

```tsx
/**
 * 길게 눌러 고르기로 들어간다. 브라우저 기본 동작을 다 막아야 한다.
 * iOS 는 이미지 롱프레스에 저장 팝업을 띄우고, 데스크톱은 텍스트가 드래그로 잡힌다.
 * 카드를 누른 채 화면을 밀면 고르기로 들어가면 안 되므로 10px 넘게 움직이면 취소한다.
 */
function useLongPress(onLong: () => void) {
  const timer = useRef<ReturnType<typeof setTimeout> | null>(null);
  const from = useRef({ x: 0, y: 0 });

  const cancel = () => {
    if (timer.current) clearTimeout(timer.current);
    timer.current = null;
  };

  return {
    onContextMenu: (e: React.MouseEvent) => e.preventDefault(),
    onPointerDown: (e: React.PointerEvent) => {
      from.current = { x: e.clientX, y: e.clientY };
      cancel();
      timer.current = setTimeout(onLong, 500);
    },
    onPointerMove: (e: React.PointerEvent) => {
      const d = Math.hypot(e.clientX - from.current.x, e.clientY - from.current.y);
      if (d > 10) cancel();
    },
    onPointerUp: cancel,
    onPointerCancel: cancel,
    onPointerLeave: cancel,
  };
}
```

- [ ] **Step 2: `sell-modal.tsx`**

고른 칸을 목록으로 보여주고 받을 크레딧을 크게 둔다. **수량 조절은 없다** — 보관함에서 이미 정했다.

- [ ] **Step 3: `solo.tsx` 에 붙인다**

`sell(picks)` 는 `takeFrom` 을 골라진 칸마다 부르고 크레딧을 더한다.

- [ ] **Step 4: 확인**

Run: `npm run dev -- -p 3100`

- 카드를 길게 누르면 고르기로 들어간다
- 카드를 누른 채 화면을 스크롤하면 고르기로 **안** 들어간다
- 헤더 "고르기" 버튼으로도 들어간다
- 핸들의 `+`/`−` 가 동작하고 0 이 되면 선택이 풀린다
- 두 칸 이상 고르면 강화 버튼이 잠긴다
- 팔면 크레딧이 정확히 늘고 그 장수만 보관함에서 빠진다
- 키보드 Tab 으로 카드에 닿고 Space 로 고를 수 있다
- 모바일 폭에서 길게 누를 때 iOS 저장 팝업이나 텍스트 선택이 안 뜬다 (데스크톱 브라우저의 모바일 에뮬레이션으로 확인)

- [ ] **Step 5: 검증과 커밋**

```bash
git add app/_solo/vault-grid.tsx app/_solo/sell-modal.tsx app/_solo/solo.tsx
git commit -m "보관함 고르기와 판매를 만든다"
```

---

### Task 5: 강화

**Files:**
- Create: `app/_solo/upgrade-overlay.tsx`
- Modify: `app/_solo/solo.tsx`

- [ ] **Step 1: 오버레이를 만든다**

설계 문서 "강화" 절 그대로. 세로로 쌓는다.

1. 위쪽에 보유 크레딧, 오른쪽 위에 닫기
2. 카드 (`size="full"`, `plus` 반영)
3. `+3 (3,277) → +4 (5,243)` — 수치 아래 그 단계의 값. **증감액은 안 쓴다**
4. `? 확률 보기` — 펼치면 아래를 밀어내며 열린다. 겹치는 툴팁은 보호권 토글을 가린다
5. 파괴 보호권 토글. 켜면 파괴가 실패로 흡수된다 (`oddsAt(plus, true)`)
6. `강화하기 🪙 1,494` — 낼 금액을 버튼 안에. **"원"을 붙이지 않는다.** 크레딧은 게임
   화폐라 현금 결제로 읽히면 안 된다

강화를 누르면 `Math.random()` 으로 굴려 `applyUpgrade` 를 부른다.

```tsx
function roll(plus: number, guard: boolean): "success" | "keep" | "destroy" {
  const o = oddsAt(plus, guard);
  const r = Math.random() * 100;
  if (r < o.success) return "success";
  if (r < o.success + o.keep) return "keep";
  return "destroy";
}
```

- [ ] **Step 2: 최고 기록을 갱신한다**

`run.best` 는 카드 가치로 비교한다. **강화 중에 터져도 기록은 남아야 한다.**

```ts
// 성공한 뒤 값이 최고 기록보다 높으면 갈아끼운다. 파괴돼도 이 기록은 안 지운다.
const value = cardValue(card.tier, plus + 1);
const bestValue = r.best ? cardValue(byId.get(r.best.id)!.tier, r.best.plus) : 0;
```

- [ ] **Step 3: 연출은 일단 최소로 둔다**

성공·실패·파괴를 글자로만 알린다. **어떻게 움직일지는 이 화면이 돌아가는 걸 보고 정한다.**
`globals.css` 에 이미 `battle-win-glow` 와 `battle-lose-shatter` 가 있어 출발점이 된다.
지금은 결과 문구와 `aria-live` 만 붙이고, 연출은 Task 7 에서 눈으로 보며 맞춘다.

- [ ] **Step 4: 확인**

- 확률 세 값의 합이 100% 로 보인다
- 보호권을 켜면 파괴가 0% 가 되고 실패가 그만큼 는다
- 낼 금액이 `upgradeCost + guardFee` 와 맞는다
- 크레딧이 모자라면 버튼이 잠긴다
- 성공하면 카드가 `+N+1` 로 바뀌고 그 한 장만 칸에서 빠져나온다
- 파괴되면 그 한 장만 사라진다
- **파괴 뒤에도 최고 기록이 남는다**
- 천장(+10)에 닿으면 더 못 올린다

- [ ] **Step 5: 검증과 커밋**

```bash
git add app/_solo/upgrade-overlay.tsx app/_solo/solo.tsx
git commit -m "강화 오버레이를 만든다"
```

---

### Task 6: 개봉 연출

**Files:**
- Create: `app/_solo/opening.tsx`
- Modify: `app/_solo/solo.tsx`

- [ ] **Step 1: 개봉 단계를 만든다**

여럿이서 모드의 흐름(`reposition` → `tear` → `drop`)을 참고하되 혼자서는 플레이어가 하나라 더 짧다. `PackShell` 을 쓴다.

산 팩이 화면 가운데서 뜯기고, 카드가 한 장씩 뒤집혀 나온 뒤 보관함에 들어간다.
에픽 이상이 나오면 여럿이서 모드처럼 등급 문구를 띄운다.

- [ ] **Step 2: 확인**

- 팩을 사면 개봉 화면으로 넘어간다
- 팩 겉모습이 상점의 그 팩과 같다
- 카드가 한 장씩 뒤집힌다
- 끝나면 보관함으로 돌아오고 카드가 쌓여 있다
- `prefers-reduced-motion` 을 켜면 연출이 즉시 끝난다 (`globals.css` 에 이미 규칙이 있다)

- [ ] **Step 3: 커밋**

```bash
git add app/_solo/opening.tsx app/_solo/solo.tsx
git commit -m "혼자서 모드 개봉 연출을 붙인다"
```

---

### Task 7: 종료와 결과, 그리고 강화 연출

**Files:**
- Create: `app/_solo/result.tsx`
- Modify: `app/_solo/solo.tsx`, `app/_solo/upgrade-overlay.tsx`, `app/globals.css`

- [ ] **Step 1: 두 가지 종료**

- **파산**: `isBankrupt(credits, vault.length)` 가 참이면 결과로 넘어간다. 매 상태 변화 뒤 확인
- **자발적 종료**: 헤더에 "여기까지 하고 결과 보기". 이게 없으면 수십 번 클릭이 남은 판에 갇힌다

- [ ] **Step 2: 결과 화면**

최고 기록 카드를 크게(`size="full"`) 보여주고 그 가치를 적는다. "다시하기"(`clearRun` 후 새 런)를 둔다.

- [ ] **Step 3: 강화 연출을 맞춘다**

여기서 실제로 움직이는 걸 보며 정한다. 출발점은 `globals.css` 의 `battle-win-glow`(승자 카드가 등급색으로 한 번 빛남)와 `battle-lose-shatter`(색이 빠지며 사라짐)다. 강화에 맞게 새 키프레임이 필요하면 더한다.

**이 단계는 사람이 보고 판단한다.** 몇 안을 만들어 보여주고 고르게 한다.

- [ ] **Step 4: 한 판 통째로 돌려본다**

새 런에서 파산할 때까지 실제로 플레이한다. 확인할 것:

- 팩을 사고 → 개봉하고 → 팔고 → 강화하는 흐름이 끊기지 않는다
- 크레딧이 어디서도 음수가 안 된다
- 파산이 실제로 온다
- 결과 화면의 최고 기록이 실제로 도달한 최고와 맞는다
- 새로고침해도 진행이 안 날아간다

- [ ] **Step 5: 커밋**

```bash
git add app/_solo/result.tsx app/_solo/solo.tsx app/_solo/upgrade-overlay.tsx app/globals.css
git commit -m "런 종료와 결과 화면을 만든다"
```

---

### Task 8: 문서

- [ ] **Step 1: `CLAUDE.md`**

라우팅 절에 `/[sport]/solo` 두 줄을 더하고, "혼자서 모드는 아직 없다" 문장을 걷어낸다.
`_solo` 층 설명에 화면 파일들도 적는다.

- [ ] **Step 2: 설계 문서**

`docs/superpowers/specs/2026-08-05-solo-mode-design.md` 의 "미룬 것" 절에서 강화 결과 연출을
빼고 실제로 정한 것을 적는다.

- [ ] **Step 3: 커밋**

```bash
git add CLAUDE.md docs/superpowers/specs/2026-08-05-solo-mode-design.md
git commit -m "혼자서 모드 화면을 문서에 반영한다"
```

---

## 완료 기준

- [ ] `npm test` 73개 통과 (화면은 테스트가 없으므로 늘지 않는다)
- [ ] `npm run lint`, `npx tsc --noEmit` 통과
- [ ] `npm run build` 통과. `/[sport]/solo`, `/[sport]/solo/[season]` 이 빌드 때 구워진다
- [ ] 메인화면에서 혼자서 두 칸이 열려 있고 잠금 표시가 없다
- [ ] 새 런에서 파산까지 한 판을 끊김 없이 플레이할 수 있다
- [ ] 새로고침해도 진행이 남는다
- [ ] 여럿이서 모드가 전과 똑같이 동작한다 (`PackShell` 을 꺼낸 여파가 없다)
- [ ] `app/_solo/` 의 파일이 각각 300줄을 넘지 않는다

## 다음 계획

자랑 링크. `/[sport]/flex/[...code]` 와 `opengraph-image.tsx`.

그때 `pools.ts` 의 무검증 캐스팅에 런타임 가드가 필요해진다. 지금은 빌드 때만 도는
경로라 형태가 어긋나면 빌드가 깨져서 오히려 안전하다. 자랑 링크는 요청 시점에 그리므로
같은 어긋남이 사용자에게 500 으로 간다.
