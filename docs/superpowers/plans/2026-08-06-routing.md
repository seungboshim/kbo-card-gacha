# 라우팅 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 메인화면을 종목 × 모드 네 칸으로 바꾸고, 시즌을 모드 안으로 넣어 `/[sport]/multi/[season]` 로 옮긴다.

**Architecture:** 시즌 매니페스트(`seasons.ts`)를 단일 출처로 삼아 메인화면·시즌 선택·게임 라우트가 모두 거기서 파생된다. 종목과 시즌이 동적 세그먼트가 되고 `generateStaticParams` 로 전부 빌드 때 미리 굽는다. 기존 `/kbo`, `/epl` 은 `next.config.ts` 의 리다이렉트로 넘긴다.

**Tech Stack:** Next.js 16, React 19, TypeScript. 새 의존성 없음.

이 계획이 끝나면 여럿이서 모드가 새 경로에서 지금과 똑같이 돌고, 메인화면이 최종 모양이 된다. 혼자서 모드는 아직 없으므로 그 두 칸은 "곧 열려요"로 잠가둔다.

**설계 문서:** `docs/superpowers/specs/2026-08-05-solo-mode-design.md`

---

## 앞 단계에서 이미 있는 것

- `app/_sports/seasons.ts` — `Season` 타입, `SEASONS` 배열(`id`/`sport`/`label`/`live`), `seasonKey()`
- `app/_sports/pools.ts` — `loadPool(key)`, `LOADERS` 맵
- `data/kbo-2026.json`, `data/epl-2526.json`
- `app/_game/game.tsx` — `<Game pool={pool} sport="kbo" />`

## File Structure

**신규**

| 파일 | 책임 |
|---|---|
| `app/[sport]/multi/page.tsx` | 시즌 고르기 |
| `app/[sport]/multi/[season]/page.tsx` | 여럿이서 게임 |

**수정**

| 파일 | 무엇을 |
|---|---|
| `app/_sports/seasons.ts` | 종목 표시 정보와 헬퍼 추가 |
| `app/_sports/pools.ts` | 시즌에 로더가 빠졌는지 모듈 로드 때 확인 |
| `app/page.tsx` | 메인화면 개편 |
| `next.config.ts` | 옛 경로 리다이렉트 |
| `CLAUDE.md` | 라우팅 절 갱신 |

**삭제**

`app/kbo/page.tsx`, `app/epl/page.tsx` — 동적 라우트와 리다이렉트가 대신한다.

시즌 선택 화면은 나중에 혼자서 모드에도 필요하지만 **지금은 `multi` 하나뿐이라 공용
컴포넌트로 빼지 않는다.** 4단계에서 두 번째 호출자가 생길 때 뺀다.

---

### Task 1: 매니페스트에 종목 표시 정보와 안전장치를 더한다

**Files:**
- Modify: `app/_sports/seasons.ts`
- Modify: `app/_sports/pools.ts`

- [ ] **Step 1: `seasons.ts` 아래에 더한다**

```ts
/**
 * 화면에 쓰는 종목 표시 정보. SportConfig(kbo.ts/epl.ts)에도 비슷한 게 있지만 그쪽은
 * 함수 필드를 물고 있어 클라이언트로 못 넘어간다. 메인화면이 쓸 수 있게 여기 따로 둔다.
 */
export const SPORT: Record<Season["sport"], { name: string; emblem: string }> = {
  kbo: { name: "KBO", emblem: "⚾" },
  epl: { name: "프리미어리그", emblem: "⚽" },
};

export const SPORT_KEYS = Object.keys(SPORT) as Season["sport"][];

export const seasonsOf = (sport: Season["sport"]) => SEASONS.filter((s) => s.sport === sport);

/** 라이브 시즌을 하나라도 가진 종목인가. 메인화면의 LIVE 배지가 이걸 따라간다. */
export const hasLive = (sport: Season["sport"]) => seasonsOf(sport).some((s) => s.live);
```

- [ ] **Step 2: `pools.ts` 에 안전장치를 건다**

시즌을 `seasons.ts` 에 더하고 `pools.ts` 로더를 빠뜨리면 런타임에야 터진다. 모듈을 읽는 순간 걸리게 한다. 파일 맨 아래에 더한다.

```ts
// 시즌을 seasons.ts 에 더하고 여기 로더를 빠뜨리는 실수를 빌드에서 잡는다.
// 두 파일을 같이 고쳐야 하는 구조라 한쪽만 고치기 쉽다.
for (const s of SEASONS) {
  if (!LOADERS[seasonKey(s)]) {
    throw new Error(`${seasonKey(s)} 의 JSON 로더가 pools.ts 에 없어요`);
  }
}
```

그리고 파일 맨 위 import 에 더한다.

```ts
import { SEASONS, seasonKey } from "./seasons";
```

- [ ] **Step 3: 안전장치가 실제로 작동하는지 확인한다**

`seasons.ts` 의 `SEASONS` 에 가짜 항목을 잠깐 넣는다.

```ts
  { id: "9999", sport: "kbo", label: "가짜", live: false },
```

Run: `npm run build`
Expected: 빌드가 `kbo-9999 의 JSON 로더가 pools.ts 에 없어요` 로 실패한다.

**확인했으면 방금 넣은 가짜 항목을 반드시 지운다.** 지운 뒤 `npm run build` 가 다시 통과하는 것까지 보고 넘어간다.

- [ ] **Step 4: 검증**

Run: `npm test && npm run lint && npx tsc --noEmit`
Expected: 39개 통과, lint 깨끗, 타입 오류 없음

- [ ] **Step 5: 커밋**

```bash
git add app/_sports/seasons.ts app/_sports/pools.ts
git commit -m "매니페스트에 종목 표시 정보와 로더 누락 방지를 더한다"
```

---

### Task 2: 게임 라우트를 동적 세그먼트로 옮긴다

**Files:**
- Create: `app/[sport]/multi/[season]/page.tsx`
- Delete: `app/kbo/page.tsx`, `app/epl/page.tsx`
- Modify: `next.config.ts`

- [ ] **Step 1: 새 라우트를 만든다**

`app/[sport]/multi/[season]/page.tsx`

```tsx
import { notFound } from "next/navigation";
import Game from "../../../_game/game";
import { loadPool } from "../../../_sports/pools";
import { SEASONS, SPORT, seasonKey, type Season } from "../../../_sports/seasons";

// 매니페스트에 없는 주소는 404. 시즌 조합이 유한하므로 전부 빌드 때 굽는다.
export const dynamicParams = false;

export function generateStaticParams() {
  return SEASONS.map((s) => ({ sport: s.sport, season: s.id }));
}

const find = (sport: string, season: string): Season | undefined =>
  SEASONS.find((s) => s.sport === sport && s.id === season);

export async function generateMetadata({ params }: { params: Promise<{ sport: string; season: string }> }) {
  const { sport, season } = await params;
  const s = find(sport, season);
  return { title: s ? `${SPORT[s.sport].name} 카드깡 ${s.label}` : "카드깡" };
}

export default async function Page({ params }: { params: Promise<{ sport: string; season: string }> }) {
  const { sport, season } = await params;
  const s = find(sport, season);
  // dynamicParams=false 라 여기까지 안 오지만, s 를 좁히려면 필요하다.
  if (!s) notFound();
  const pool = await loadPool(seasonKey(s));
  return <Game pool={pool} sport={s.sport} />;
}
```

- [ ] **Step 2: 옛 페이지를 지운다**

```bash
git rm app/kbo/page.tsx app/epl/page.tsx
```

`app/kbo/`, `app/epl/` 디렉터리가 비면 같이 사라진다. `git status` 로 다른 게 딸려가지 않았는지 본다.

- [ ] **Step 3: 리다이렉트를 건다**

`next.config.ts` 의 `nextConfig` 객체 안, `images` 옆에 더한다.

```ts
  // 옛 주소를 새 경로로 넘긴다. 리다이렉트는 파일시스템보다 먼저 검사된다.
  // 308(영구)이 아니라 307(임시)인 이유는, 나중에 /kbo 를 종목 홈으로 쓰고 싶어질 때
  // 브라우저 캐시에 박혀 있으면 되돌리기 어렵기 때문이다.
  async redirects() {
    return [
      { source: "/kbo", destination: "/kbo/multi", permanent: false },
      { source: "/epl", destination: "/epl/multi", permanent: false },
    ];
  },
```

- [ ] **Step 4: 빌드해서 경로가 다 잡히는지 본다**

Run: `npm run build`

Expected: 빌드 성공. 라우트 표에 `/kbo/multi/2026` 과 `/epl/multi/2526` 이 정적(`○`)으로 잡힌다. 라우트 표를 그대로 보고서에 붙인다.

이 시점에는 `/[sport]/multi` (시즌 선택)가 아직 없어서 `/kbo/multi` 로 가는 리다이렉트가 404 로 떨어진다. **정상이다.** Task 3 에서 만든다.

- [ ] **Step 5: 게임이 도는지 확인한다**

Run: `npm run dev -- -p 3100`

`http://localhost:3100/kbo/multi/2026` 과 `http://localhost:3100/epl/multi/2526` 을 연다. 확인할 것:

- 카드팩 개봉이 전과 똑같이 동작한다 (팩 고르기 → 뜯기 → 카드 뒤집기)
- 카드에 선수 사진과 팀 로고가 뜬다
- 탭 제목이 각각 `KBO 카드깡 2026`, `프리미어리그 카드깡 25/26` 이다
- `http://localhost:3100/kbo/multi/9999` 는 404 다

브라우저를 못 몰면 보고서에 그렇게 적는다. 지어내지 않는다.

- [ ] **Step 6: 검증과 커밋**

Run: `npm test && npm run lint && npx tsc --noEmit`

```bash
git add "app/[sport]/multi/[season]/page.tsx" next.config.ts
git commit -m "게임 라우트를 종목·시즌 동적 세그먼트로 옮긴다"
```

`git rm` 한 두 파일은 이미 스테이징돼 있으므로 같이 들어간다. 커밋 뒤 `git show --stat` 으로 삭제 두 건이 포함됐는지 확인한다.

---

### Task 3: 시즌 선택 화면

**Files:**
- Create: `app/[sport]/multi/page.tsx`

- [ ] **Step 1: 화면을 만든다**

```tsx
import Link from "next/link";
import { notFound } from "next/navigation";
import { SPORT, SPORT_KEYS, seasonsOf } from "../../_sports/seasons";

export const dynamicParams = false;

export function generateStaticParams() {
  return SPORT_KEYS.map((sport) => ({ sport }));
}

export async function generateMetadata({ params }: { params: Promise<{ sport: string }> }) {
  const { sport } = await params;
  const s = SPORT[sport as keyof typeof SPORT];
  return { title: s ? `${s.name} 여럿이서 · 시즌 고르기` : "카드깡" };
}

export default async function Page({ params }: { params: Promise<{ sport: string }> }) {
  const { sport } = await params;
  if (!(sport in SPORT)) notFound();
  const key = sport as keyof typeof SPORT;
  const seasons = seasonsOf(key);

  return (
    <main className="mx-auto flex min-h-screen w-full max-w-lg flex-col justify-center gap-8 px-4 py-10">
      <div>
        <Link href="/" className="text-sm text-zinc-500 transition-colors hover:text-zinc-300">
          ← 카드깡
        </Link>
        <h1 className="mt-3 text-2xl font-black tracking-tight">
          {SPORT[key].emblem} {SPORT[key].name} · 여럿이서
        </h1>
        <p className="mt-1.5 text-sm text-zinc-400">시즌을 골라주세요</p>
      </div>

      <div className="flex flex-col gap-3">
        {seasons.map((s) => (
          <Link
            key={s.id}
            href={`/${sport}/multi/${s.id}`}
            className="rounded-2xl bg-white/5 px-5 py-4 ring-1 ring-white/10 transition-colors hover:bg-white/10 focus-visible:ring-2 focus-visible:ring-white/70 focus-visible:outline-none"
          >
            <div className="flex items-center gap-2">
              <span className="text-xl font-black tabular-nums">{s.label}</span>
              {s.live && (
                <span className="rounded-full bg-rose-500/15 px-2 py-0.5 text-[10px] font-bold text-rose-300 ring-1 ring-rose-400/40 ring-inset">
                  LIVE
                </span>
              )}
            </div>
            <p className="mt-1 text-xs text-zinc-400">
              {s.live
                ? "매일 기록이 갱신돼요. 어제보다 등급이 오른 선수는 카드에 표시가 붙어요."
                : "시즌이 끝나 등급이 고정된 카드예요."}
            </p>
          </Link>
        ))}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: 빌드와 화면 확인**

Run: `npm run build`
Expected: `/kbo/multi` 와 `/epl/multi` 가 정적으로 잡힌다. 라우트 표를 붙인다.

Run: `npm run dev -- -p 3100`

- `http://localhost:3100/kbo/multi` 에 2026 카드 하나가 LIVE 배지와 함께 뜬다
- `http://localhost:3100/epl/multi` 에 25/26 카드가 배지 없이 뜬다
- 시즌을 누르면 게임으로 넘어간다
- `http://localhost:3100/kbo` 가 `/kbo/multi` 로 넘어간다 (리다이렉트가 이제 착지한다)
- `← 카드깡` 을 누르면 메인으로 간다

- [ ] **Step 3: 검증과 커밋**

Run: `npm test && npm run lint && npx tsc --noEmit`

```bash
git add "app/[sport]/multi/page.tsx"
git commit -m "시즌 고르기 화면을 만든다"
```

경로에 대괄호가 있어 셸이 글롭으로 먹으므로 **따옴표로 감싼다.**

---

### Task 4: 메인화면 개편

**Files:**
- Modify: `app/page.tsx` (전체 교체)

- [ ] **Step 1: 화면을 만든다**

```tsx
import Link from "next/link";
import { SPORT, SPORT_KEYS, hasLive } from "./_sports/seasons";

// 입구 카드 주색: 리그 색으로 두 카드를 한눈에 가른다.
// 리그 원색(KBO 남색 #0F2B5B, PL 퍼플 #3D195B)은 어두운 배경 위에서 alpha 를 올려도 안 보인다.
// 그래서 같은 색조의 밝은 쪽을 쓴다.
const LEAGUE_COLOR: Record<string, string> = { kbo: "#3B82F6", epl: "#A855F7" };

// ready:false 인 모드는 잠긴 카드로 그린다. 혼자서 모드는 다음 단계에서 열린다.
const MODES = [
  {
    key: "solo",
    title: "혼자서",
    desc: "주어진 돈으로 카드팩을 사고 카드깡을 한 뒤 선수를 강화해봐요",
    ready: false,
  },
  {
    key: "multi",
    title: "여럿이서",
    desc: "친구들과 함께 카드깡하고, 뽑은 카드로 대결해요",
    ready: true,
  },
] as const;

export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-3xl flex-col justify-center gap-12 px-4 py-10">
      <h1 className="text-center text-2xl font-black tracking-tight sm:text-3xl">카드깡</h1>

      {MODES.map((mode) => (
        <section key={mode.key} className="flex flex-col gap-3">
          <div>
            <h2 className="text-lg font-bold">
              {mode.title}
              {!mode.ready && <span className="ml-2 text-xs font-medium text-zinc-500">곧 열려요</span>}
            </h2>
            <p className="mt-0.5 text-sm text-zinc-400">{mode.desc}</p>
          </div>

          <div className="grid gap-3 sm:grid-cols-2">
            {SPORT_KEYS.map((sport) => {
              const color = LEAGUE_COLOR[sport];
              const inner = (
                <>
                  <span aria-hidden="true" className="text-5xl leading-none select-none sm:text-6xl">
                    {SPORT[sport].emblem}
                  </span>
                  <span className="text-base font-bold">{SPORT[sport].name}</span>
                  {hasLive(sport) && (
                    <span className="absolute top-3 right-3 rounded-full bg-rose-500/15 px-2 py-0.5 text-[10px] font-bold text-rose-300 ring-1 ring-rose-400/40 ring-inset">
                      LIVE
                    </span>
                  )}
                </>
              );
              const shell =
                "relative flex flex-col items-center justify-center gap-2.5 overflow-hidden rounded-2xl border px-6 py-8";
              const style = {
                background: `radial-gradient(130% 100% at 50% -5%, ${color}80 0%, ${color}1f 55%, transparent 80%), rgba(255,255,255,0.03)`,
                borderColor: `${color}99`,
                boxShadow: `0 0 40px -10px ${color}80`,
              };

              return mode.ready ? (
                <Link
                  key={sport}
                  href={`/${sport}/${mode.key}`}
                  className={`${shell} transition-transform hover:scale-[1.02] active:scale-[0.96] focus-visible:ring-2 focus-visible:ring-white/70 focus-visible:outline-none`}
                  style={style}
                >
                  {inner}
                </Link>
              ) : (
                <div key={sport} aria-disabled="true" className={`${shell} opacity-40`} style={style}>
                  {inner}
                </div>
              );
            })}
          </div>
        </section>
      ))}
    </main>
  );
}
```

- [ ] **Step 2: 빌드와 화면 확인**

Run: `npm run build && npm run dev -- -p 3100`

`http://localhost:3100/` 에서 확인할 것:

- 위가 **혼자서**(곧 열려요, 흐림), 아래가 **여럿이서** 순서다
- 각 섹션에 KBO·프리미어리그 두 칸
- KBO 두 칸에만 LIVE 배지가 붙는다 (`seasons.ts` 의 `live: true` 를 따라간다)
- 여럿이서 칸을 누르면 `/kbo/multi` 로 간다
- 혼자서 칸은 눌러도 아무 일이 없다
- 좁은 화면(모바일 폭)에서 칸이 세로로 쌓인다

- [ ] **Step 3: 검증과 커밋**

Run: `npm test && npm run lint && npx tsc --noEmit`

```bash
git add app/page.tsx
git commit -m "메인화면을 종목 × 모드 네 칸으로 바꾼다"
```

---

### Task 5: 문서 갱신

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 구조 절의 라우팅 설명을 고친다**

`CLAUDE.md` 의 `### 서버 → 클라이언트 경계` 절 첫 문단이 아직 `app/kbo/page.tsx`, `app/epl/page.tsx` 를 가리킨다. 그 파일들은 지워졌다. 이렇게 바꾼다.

```markdown
### 서버 → 클라이언트 경계

`app/[sport]/multi/[season]/page.tsx` 는 서버 컴포넌트다. `loadPool()` 로 저장소의 JSON 을
읽어 `<Game pool={pool} sport="kbo" />` 로 넘긴다. 요청 시점에 받아오는 게 없어 정적으로
미리 만들어진다. `SportConfig` 에 함수 필드(`miniStatKeys`)가 있어 직렬화로 못 넘어가므로
**key 문자열만 넘기고 `game.tsx` 가 다시 찾는다**. 이 구조를 깨지 말 것.
```

- [ ] **Step 2: 라우팅 절을 새로 더한다**

`### 서버 → 클라이언트 경계` 절 **앞에** 넣는다.

````markdown
### 라우팅

```
/                          메인. 종목 × 모드
/[sport]/multi             시즌 고르기
/[sport]/multi/[season]    여럿이서 게임
```

종목과 시즌은 동적 세그먼트다. 둘 다 `_sports/seasons.ts` 의 `SEASONS` 에서 파생되고
`generateStaticParams` 로 빌드 때 전부 굽는다. `dynamicParams = false` 라 매니페스트에
없는 주소는 404 다.

옛 주소 `/kbo`, `/epl` 은 `next.config.ts` 의 `redirects()` 가 `/[sport]/multi` 로 넘긴다.

혼자서 모드(`/[sport]/solo`)는 아직 없다. 메인화면에서 잠긴 칸으로만 보인다.
````

- [ ] **Step 3: 시즌 추가 절차를 고친다**

`CLAUDE.md` 의 "시즌을 추가할 때" 문단에 `pools.ts` 안전장치를 적는다.

```markdown
**시즌을 추가할 때**는 `_sports/seasons.ts` 의 `SEASONS` 와 `_sports/pools.ts` 의 `LOADERS` 에
한 줄씩 더하고 스냅샷을 굽는다. 한쪽만 고치면 `pools.ts` 가 모듈을 읽는 순간 던져서
빌드가 깨진다.
```

- [ ] **Step 4: 적은 경로가 실제로 있는지 대조한다**

Run: `ls "app/[sport]/multi/page.tsx" "app/[sport]/multi/[season]/page.tsx" app/_sports/seasons.ts app/_sports/pools.ts`
Expected: 넷 다 존재. `app/kbo/`, `app/epl/` 은 없어야 한다.

- [ ] **Step 5: 커밋**

```bash
git add CLAUDE.md
git commit -m "라우팅 개편을 문서에 반영한다"
```

---

## 완료 기준

- [ ] `npm test` 39개 통과
- [ ] `npm run lint`, `npx tsc --noEmit` 통과
- [ ] `npm run build` 통과. `/`, `/kbo/multi`, `/epl/multi`, `/kbo/multi/2026`, `/epl/multi/2526` 이 전부 빌드 때 구워진다.
      `generateStaticParams` 를 쓰는 라우트는 `○` 가 아니라 `●`(SSG) 로 표시된다. 둘 다 정적이다
- [ ] `/kbo` 가 `/kbo/multi` 로 넘어간다
- [ ] `/kbo/multi/9999` 가 404 다
- [ ] 두 종목 다 카드팩 개봉이 전과 똑같이 동작한다
- [ ] 메인화면에서 KBO 칸에만 LIVE 배지가 붙는다

## 다음 계획

경제 로직(`economy.ts`, `storage.ts`, 보관함 묶기). 화면 없이 `node --test` 로만 검증한다.
